---
description: Who is holding what, at which stage, and each person's real turnaround from received to client-told.
---

1. Run `npm run ato -- workload`.
2. Turnaround is measured received-to-notified, per person, from the records - not sampled, not estimated. Seven days is the standard `/compliance` enforces.
3. If one person's `oldest held` is climbing, list their pipeline (`register --status=matched --json`, filter by staff) and say what is stuck, oldest first.
4. This is a management read, not a leaderboard: pair it with the inbox so the conversation is about specific documents, not averages.
